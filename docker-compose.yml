version: '3.8'

services:
  # Frontend (React Vite App)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: platypus_walk_landing_frontend
    restart: unless-stopped
    environment:
      # nginx-proxy configuration for domain routing
      - VIRTUAL_HOST=theplatypus.in,www.theplatypus.in
      - VIRTUAL_PORT=80
      # Let's Encrypt SSL configuration
      - LETSENCRYPT_HOST=theplatypus.in,www.theplatypus.in
      - LETSENCRYPT_EMAIL=sagar@quantalynk.com
      # Supabase configuration
      - VITE_SUPABASE_URL=https://jhckzuxlkggbkbrtjjdo.supabase.co
      - VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImpoY2t6dXhsa2dnYmticnRqamRvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTYzNjM0NTMsImV4cCI6MjA3MTkzOTQ1M30.53_OwRcIN5NGSRV5YTfLvgqUFl8a0EKWwGjFl0OUWj8
    expose:
      - "80"
    networks:
      - platypus_walk_network
      - nginx-proxy_default
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  platypus_walk_network:
    driver: bridge
  nginx-proxy_default:
    external: true
